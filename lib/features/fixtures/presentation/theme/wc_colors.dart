import 'package:flutter/material.dart';

class WcColors {
  // Fondo y superficies
  static const bgLight = Color(0xFF0D1B2A);
  static const cardBg = Color(0xFF1A2F4A);
  static const surface = Color(0xFF243B55);

  // Acentos FIFA
  static const gold = Color(0xFFFFC300);
  static const tealPrimary = Color(0xFF00CFB5);
  static const tealSelected = Color(0xFF00A88F);

  // Texto
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFF8FA8C0);
  static const textMuted = Color(0xFF6B8AA0);
  static const textLive = Color(0xFF00CFB5);

  // Estados
  static const live = Color(0xFFE53935);
  static const success = Color(0xFF27AE60);

  // Bordes / divisores
  static const divider = Color(0xFF2A4A6A);

  // Aliases compatibilidad
  static const teal = tealPrimary;
  static const yellow = gold;
  static const navy = cardBg;
  static const navyDark = bgLight;
  static const textDark = textPrimary;
  static const accentGold = gold;
}
